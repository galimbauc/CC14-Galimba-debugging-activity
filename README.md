# CC14-Unit-3-Debugging-Activity

### HTML Changes
* Added missing `</a>` tag to the About link.
* Added missing `</h2>` tag to the About Us heading.
* Closed the `<form>` tag in the Contact section.
* Corrected broken tags and formatting in the form area.
* Cleaned up invisible non-breaking space characters causing layout errors.

### CSS Changes
* Added missing opening brace `{` for the body selector.
* Corrected the header selector spelling from `heade` to `header`.
* Added missing closing brace `}` for the main section rule.
* Added missing semicolon after `bottom: 0` in the footer.
* Added missing semicolon after `text-align: center` in the footer.
